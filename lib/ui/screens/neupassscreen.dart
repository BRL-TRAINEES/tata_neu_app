import 'package:flutter/material.dart';

class Neupassscreen extends StatelessWidget {
  const Neupassscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
